SELECT
    listings.price,
    lsitings.id,
    Groupby (calender.date) 4,
    listings.availbity_365
FROM
    lisitngs
JOIN
    calendar
ON
    listings.id = calender.id 

Group by lsitings.date
  
  
